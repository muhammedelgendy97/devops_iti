#!/usr/bin/env groovy

@Library('shared-library') _

pipeline {
    agent any
    tools {
        maven 'mvn'
    }
    stages {
        stage("build jar") {
            steps {
                script {
                   buildJar()
                }
            }
        }
        stage("deploy") {
            steps {
                script {
                   deployFunc()
                }
            }
        }
    }
}
