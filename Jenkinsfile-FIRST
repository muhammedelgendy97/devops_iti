#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('build') {
            when {
                expression {
                    BRANCH_NAME == 'main'
                }
            }
            steps {
                script {
                    echo "Building the application "
                }
            }
        }
        stage('test') {
      
            steps {
                script {
                    echo "Testing the application..."
                }
            }
        }
        stage('deploy') {
            when {
                expression {
                    BRANCH_NAME == 'main'
                }
            }
            steps {
                script {
                    sshagent(['ec2-server-key']) {
                        def dockerCmd= 'docker run -d -p 8081:8081 --name nexus sonatype/nexus3'
                        "ssh -o StrictHostKeyChecking=no ubuntu@100.25.170.135 ${dockerCmd}"
                }
                 
                }
            }
        }
    }
}
